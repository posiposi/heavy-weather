// Package domain は天気通知の中核となる値オブジェクトと降水判定ロジックを持つ。
//
// このパッケージは実行環境を知らない。aws-lambda-go を import した時点で
// 設計が誤っているという判定基準になる（CLAUDE.md「アーキテクチャ上の不変条件」）。
//
// 実体は設計書 §9 第1段階で追加する。現時点では go test ./... が対象とする
// パッケージを1つ用意するためだけに存在する。
package domain
